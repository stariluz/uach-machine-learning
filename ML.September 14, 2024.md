
- Why use MinMaxScaler and not Normalizer in Python?
  **Answer in  September 21 class:** Normalizer works in other ambits, and the normalization is with rows. MinMaxScaler by default works as a Normalizer, but it works with columns, thats why.

- What it is binary encoding?
  With label encoding we asign a number to each category, and we transform this value to binary. It is preferable when we have a lot of categories.

- What is a class?
  It means the clasification we have about the data, if there are dogs, cats, turtles for example.
  It means the data we have about something. If we have a lot of data of one case, but so little for the other, we have unbalanced classes.


Vamos a hacer algoritmos de regresión y de clasificación.